# Notification-delivery-microservice
A standalone backend service that handles multi-channel notification delivery — in-app (WebSocket), email (SMTP), and push (simulated) — with priority-based async processing, rate limiting, retry logic, and a real-time operator dashboard.
